# OA V1.4.2 专项修复验证报告

- 验证日期：2026-08-02
- 测试环境：`http://192.168.3.117`
- 测试账号：`guoys`（管理员）
- 数据策略：保留测试数据，未执行清库或迁移

## 1. 版本与文档

| 检查项 | 结果 |
| --- | --- |
| 后端版本 | `v1.4.2` |
| 前端离线兜底版本 | 已由 `v1.4.1` 更新为 `v1.4.2` |
| 页面版本展示 | Chrome 实测显示 `v1.4.2` |
| README | 已更新到 `v1.4.2`，补充当前功能与版本里程碑 |

## 2. 前端首屏优化

- 移除入口处全量 `ElementPlus` 和全部图标注册，改为组件按需导入。
- 使用 `ElConfigProvider` 保留中文语言包。
- 删除无效的 `xlsx-vendor` 手工分包。
- 构建成功，原约 `1051 KB` 的 Element Plus 主包已拆分，当前最大 Element Plus 分包约 `77.10 KB`。
- 当前入口业务包约 `256.92 KB`；ECharts 独立分包约 `649.65 KB`，不会与基础组件一起进入首屏主包。

## 3. V1.4.2 业务复测

### 客户保存与跟进

- 客户保存成功：`V1.4.2复测客户-V142-1785657322755`，数据库 ID `7`，状态 `active`。
- 客户跟进保存成功：`V1.4.2客户跟进保存复测-V142-1785657322755`。
- 页面保存结果与数据库记录一致。

### 领料提交与库存

- 领料提交成功，未再出现“表单未初始化”。
- 审批单：`OPS-2026-0001`，状态 `approved`。
- 测试物料：`V142-MAT-1785657584820`，入库 `20`，领料 `1`，当前库存 `19`。
- 出库流水：`MR-20260802-0001`，类型 `out`，剩余库存 `19`。
- 领料总数页面不再显示错误负号。

### 加班审批状态同步

- 新建加班申请：`OPS-2026-0003`，业务记录 `overtime_requests.id=4`。
- Chrome 从运营审批中心真实点击“通过”，页面提示“已通过”。
- 审批表状态：`approved`；加班业务表状态：`approved`。
- 审批人：`guoys`（用户 ID `2`）；审批意见：`同意`。
- 填写日期 `2026-08-04`，列表显示 `2026-08-04`，已修复 UTC 序列化导致少一天的问题。
- 历史测试单 `OPS-2026-0002` 已回填业务状态，未删除原记录。

## 4. 自动化验证

- PHP 控制器与测试文件语法检查通过。
- Laravel `optimize:clear`、`config:cache`、`route:cache` 执行成功。
- `ApprovalFlowTest`：`8 passed`，共 `29 assertions`。
- 覆盖运营审批创建、通过、驳回、转交、重复操作拦截和加班业务状态同步。
- 前端 `npm run build` 成功。

## 5. 本次额外修复

- 运营审批驳回时同步保存审批意见，避免状态已驳回但 `comment` 为空。
- 加班审批最终通过或驳回时，同步 `overtime_requests.status`、`approver_id`、`approved_at`。
- 加班日期按 `Asia/Shanghai` 格式化，避免日期跨时区偏移。
- 领料数量移除错误的负号展示。

## 6. 结论

本次指定的版本一致性、README、Element Plus 首屏体积和 V1.4.2 旧问题复测均已完成。客户保存、客户跟进、领料提交、库存扣减和新建加班审批状态同步在 117 环境验证通过，页面结果与数据库状态一致。
