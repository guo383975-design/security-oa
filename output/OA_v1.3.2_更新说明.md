# OA 安防运维系统 · 更新说明

| 项目 | 内容 |
| --- | --- |
| 系统名称 | 安防运维 OA 系统（pc-web + pc-api） |
| 版本号 | **v1.3.2** |
| 发布日期 | 2026-08-02 |
| 部署环境 | 公网展示服务器 `oa.afjsw.cn`（152.136.115.121） |
| 代码来源 | 内网测试环境 `192.168.3.117` |
| 技术栈 | Laravel 11 + Vue3/Vite + PostgreSQL + Redis + Nginx |

---

## 一、更新概述

本次将内网测试环境（117）的最新源码**完整同步**至公网展示服务器，替换原有程序代码；**保留展示服务器上的全部业务数据、环境配置（`.env`）与上传文件（`storage`）**，仅补充必要的数据库表结构。

同步后展示服务器与测试环境代码完全一致（版本 `v1.3.2`），可用于对外演示与验收。

---

## 二、主要变更

### 1. 源码同步
- 前端（pc-web 构建产物）与后端（pc-api，含依赖）整体覆盖至展示服务器。
- 版本号统一为 `v1.3.2`，与测试环境保持一致。

### 2. 缺陷修复
- **修复「磁盘/存储统计」接口异常**：`/api/disk/stats` 因目标环境缺少 `disk_settings` 数据表导致 `500` 错误，已补全该表结构。
- **修复系统设置迁移失败**：`system_settings` 表缺少 `created_at` 字段，导致相关迁移在新环境执行报错，已补齐字段后迁移成功。

### 3. 数据完整性
- 本次迁移**仅补充表结构，未改动任何业务数据**。
- 原有账号、项目、工单、库存、客户等业务数据全部保留，无丢失。

---

## 三、数据保留核验

| 数据对象 | 同步前 | 同步后 | 结论 |
| --- | ---: | ---: | --- |
| 用户 users | 102 | 102 | 一致 |
| 角色 roles | 5 | 5 | 一致 |
| 权限 permissions | 111 | 111 | 一致 |
| 项目 projects | 150 | 150 | 一致 |
| 工单 work_orders | 150 | 150 | 一致 |
| 库存记录 stock_records | 202 | 202 | 一致 |
| 客户 customers | 150 | 150 | 一致 |
| 系统设置 system_settings | 11 | 12 | 仅新增 1 项配置（期初余额锁定开关），属正常演进 |

---

## 四、账号与权限

管理员账号已恢复并可用：

| 项目 | 值 |
| --- | --- |
| 用户名 | `admin` |
| 初始密码 | `admin123` |
| 角色 | 业务管理员（`admin`） |

> ⚠️ **安全提示**：当前为演示/展示环境，使用了统一弱密码。建议在正式生产部署时：
> 1. 首次登录后立即修改密码；
> 2. 启用强密码策略与登录限流；
> 3. 使用独立数据库，避免与演示数据混用。

---

## 五、访问与验证

- **访问地址**：https://oa.afjsw.cn/
- **已验证项**：
  - 首页访问正常（HTTP 200，HTTPS 强制跳转）；
  - 管理员 `admin/admin123` 登录成功；
  - 核心接口正常返回：`/api/dashboard/overview`、`/api/settings`、`/api/projects`、`/api/finance/accounts`、`/api/disk/stats` 均为 200；
  - 旧密码 `123456` 已失效（返回 401）。

---

## 六、已知限制与后续

1. 展示服务器数据为**非生产样例数据**，仅供演示与功能验收。
2. 如需正式生产部署，建议采用独立主机、独立数据库及更严格的账号与网络安全策略。
3. 历史部署包备份留存于服务器 `/tmp/oa152_bak_20260802_084919`，如需回滚可参照内部部署文档操作。

---

## 七、源码开源发布

为便于二次开发与透明交付，本次将系统源码整理为纯净公开版本，并于 **2026-08-02** 正式开源发布至 GitHub 公开仓库（共 829 个源码文件，仅含核心源码）：

- **仓库地址**：https://github.com/guo383975-design/security-oa
- **可见性**：Public（完全公开）
- **版本**：v1.3.2
- **包含目录**：`pc-api`（Laravel 后端）、`pc-web`（Vue3 前端源码）、`install.sh`（Ubuntu 一键部署脚本）、README、`LICENSE` 及配置文件（`.gitignore` / `.gitattributes`）
- **一键部署**：仓库内置 `install.sh`，在干净的 Ubuntu 22.04 / 24.04 上执行 `sudo bash install.sh --domain 你的域名` 即可自动安装 PHP 8.5 + Nginx + PostgreSQL + Redis、拉取源码、构建前端并启动（数据库密码与应用密钥随机生成，详见仓库 README）
- **已严格排除（安全）**：`.env` 及任何含密码的配置文件、数据库备份（`.sql`）、`.workbuddy` 工作目录、依赖与构建产物（`vendor` / `node_modules` / `dist`）、调试脚本、部署目录（`oa-docker` / `oa-baremetal` / `deploy`）、提交检查（`hooks` / `.pre-commit-config.yaml`）、桌面端空壳（`pc-desktop`）及 AI 配置（`.codex`）

> ⚠️ **安全说明**：公开仓库不含任何明文密码、数据库备份或业务数据。原仓库历史中曾误包含 `.env.bak` 与 `.sql` 文件，本次已通过删除并重建仓库的方式彻底清除，请以当前公开版本为准。

---

*本文档由部署流程自动汇总生成，供发布与验收使用。*
