# OA 系统 Review 报告

- **日期**: 2026-08-26
- **环境**: 117 测试机（192.168.3.117）
- **版本**: v1.4.2
- **方式**: 4 维端到端扫描（API 全量实跑 + 集成登录 + 数据层核对）

---

## 一、结论

| 维度 | 结果 |
|---|---|
| API（GET 374 接口实跑） | **500 = 0**（修复后），200 = 276，其余为参数占位/权限/校验预期响应 |
| 500 服务端错误 | **2 个 → 已全部修复** |
| 集成（多角色登录） | 仅 guoys 可登录（117 无 admin/manager/system/finance 测试账号，见 WARN-2） |
| 新模块纳入扫描 | 工具使用单（5 接口）+ 固定资产（17 接口）全部通过 |

**扫描发现 2 个真实 500 BUG，已定位根因并修复上线。**

---

## 二、已修复 BUG（2）

### BUG-1：`GET /api/customers/1/devices` → 500
- **根因**：`CustomerService::customerDevices()` 查询 `service_orders` 表时误写列名 `device_id`，实际列名是 **`customer_device_id`**（模型 fillable 与关联都是后者）。
- **报错**：`SQLSTATE[42703] Undefined column: "device_id"`
- **修复**：`device_id` → `customer_device_id`（whereNotNull + select）。
- **影响面**：客户详情页的"设备/服务记录"列表打开即 500。

### BUG-2：`GET /api/projects/payment-calendar` → 500
- **根因**：`ProjectController::paymentCalendar()` 用 `where('type','standalone')` 筛选独立付款单，但 **`finance_payments` 表从未建 `type` 列**；同时 `FinanceController::storePayment()` 写入的 `'type'=>'standalone'` 因不在 fillable 被静默丢弃。
- **报错**：`SQLSTATE[42703] Undefined column: "type"`
- **修复**：
  1. 新增幂等迁移 `2026_08_26_000001` 给 `finance_payments` 加 `type` 列（`varchar(30) default 'linked'`）；
  2. `FinancePayment::fillable` 补 `type`，新建独立付款单正确写入 `standalone`。
- **影响面**：项目详情"付款日历"页打开即 500。

---

## 三、WARN（建议关注，非阻塞）

| # | 项目 | 说明 |
|---|---|---|
| WARN-1 | 8 个 GET 接口返回 422 | `schedules`、`schedules/smart-suggest`、`step-photos`、`attendance/report`、`employee-resignations/settlement-preview`、`follow-ups/calendar`、`construction/external-works/1/bids`、`warranty-service-orders/technician-stats`。均为控制器对**查询参数必填校验**（扫描不带参触发），前端带参调用正常；建议抽查对应页面确认 |
| WARN-2 | 集成维度账号缺失 | 117 仅 `guoys` 可登录；`admin/manager/system/finance` 等测试账号不存在 → 无法自动验证角色权限矩阵/越权边界。建议后续按需创建测试角色账号 |
| WARN-3 | 3 个 portal 接口 429 限流 | `/api/portal/*` 被 429 —— 扫描器并发触发 IP 限流，非真实用户场景，忽略 |

---

## 四、扫描器改进（本次顺带修复）

`oa_full_scan.py` 路由提取正则只匹配 `Route::prefix('xxx')` 开头写法，**漏掉 `->prefix('xxx')` 链式写法**（analytics.php 等），导致 10 个 `/api/analytics/*` 接口被解析成错误路径误报 404。
- 已修正正则 `(?:Route::|->)prefix\(...\)`，重扫后 404 从 76 → 68，且无新增 500/异常。
- **经验**：路由解析类工具必须覆盖链式写法，否则产生假阳性。

---

## 五、说明

- 404（68 个）绝大部分是 `{id}=1` 参数占位替换后资源不存在（117 数据少），属预期；真实调用带真实 id 正常。
- 403（17 个）为权限不足（如 `/api/system/roles` 仅 system 账号可访问），预期行为。
- 本次修复代码：`CustomerService.php`、`FinancePayment.php`、迁移 `2026_08_26_000001_add_type_to_finance_payments.php`，已部署 117 并验证两个接口恢复 200。
